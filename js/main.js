$(document).ready(function() {
    CKEDITOR.replace( 'news-creator', {
        language: 'uk'
    });
    // Initialize Firebase

  //
  // var storage = firebase.storage();
  // var storageRef = storage.ref();
  // var pathReference = storage.ref('text/39.jpg');
  // pathReference.getDownloadURL().then(function(url) {
  //     console.log(url);
  // });

});
